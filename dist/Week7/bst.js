"use strict";
// class BSTNode {
//   val: number;
//   left: BSTNode | null;
//   right: BSTNode | null;
//   constructor(val: number, left: BSTNode | null, right: BSTNode | null) {
//     this.val = val;
//     this.left = left;
//     this.right = right;
//   }
// }
// class BST {
//   root: BSTNode | null;
//   constructor(root: BSTNode | null = null) {
//     this.root = root;
//   }
// 	addNode(val: number) {
// 		let head = this.root;
// 		const bnode = new BSTNode(val, head?.left, head?.right);
// 		if (head === null) {
// 			head = node;
// 			return;
// 		}
// 		while (head) {
// 			if (val < head.val) {
// 			}
// 		}
// 	}
//   search(val: number): BSTNode | null {
// 		let head = this.root;
// 		if (head === null) return null;
// 		while (head) {
// 			if (head.val > val) {
// 				head = head.right;
// 			}
// 		}
//   }
// }
